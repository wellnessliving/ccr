repositories{
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    // Order is important!
    compile(name:'dc-sdk', ext:'aar') // Should go first.
    compile(name:'dc-idt', ext:'aar')

    compile(name:'nmi', ext:'aar')
}